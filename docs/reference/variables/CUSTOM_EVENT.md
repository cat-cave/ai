---
id: CUSTOM_EVENT
title: CUSTOM_EVENT
---

# Variable: CUSTOM\_EVENT

```ts
const CUSTOM_EVENT: object;
```

Defined in: [packages/ai/src/custom-events.ts:17](https://github.com/TanStack/ai/blob/main/packages/ai/src/custom-events.ts#L17)

Well-known CUSTOM event names.

## Type Declaration

### APPROVAL\_REQUESTED

```ts
readonly APPROVAL_REQUESTED: "approval.requested" = 'approval.requested';
```

### APPROVAL\_RESOLVED

```ts
readonly APPROVAL_RESOLVED: "approval.resolved" = 'approval.resolved';
```

### ARTIFACT\_CREATED

```ts
readonly ARTIFACT_CREATED: "artifact.created" = 'artifact.created';
```

### FILE\_CHANGED

```ts
readonly FILE_CHANGED: "file.changed" = 'file.changed';
```

### PORT\_OPENED

```ts
readonly PORT_OPENED: "port.opened" = 'port.opened';
```

### PROCESS\_STDERR

```ts
readonly PROCESS_STDERR: "process.stderr" = 'process.stderr';
```

### PROCESS\_STDOUT

```ts
readonly PROCESS_STDOUT: "process.stdout" = 'process.stdout';
```

### SANDBOX\_CREATED

```ts
readonly SANDBOX_CREATED: "sandbox.created" = 'sandbox.created';
```

### SANDBOX\_RESUMED

```ts
readonly SANDBOX_RESUMED: "sandbox.resumed" = 'sandbox.resumed';
```
